spring:
  datasource:
    url: ${DB_URL}
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database: MySql
    database-platform: org.hibernate.dialect.MariaDBDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true


